import { Typography } from '@material-ui/core';

/**
 * - insert cast member
 *   - nome, cognome, data nascita
 *   - add button + select for movies he was part of
 */

const Cast = () => {
  return <Typography>Cast</Typography>;
};

export default Cast;
