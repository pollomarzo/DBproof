export const API_URL = process.env.NODE_ENV === "development" ? 'http://localhost:5000' : "http://34.65.94.226";
export const ACCOUNT_URL = `${API_URL}/account`;
export const PROFILE_URL = `${API_URL}/profile`;
export const CAST_URL = `${API_URL}/cast`;
export const ITEM_URL = `${API_URL}/item`;
export const FEEDBACK_URL = `${API_URL}/feedback`;
export const PLAYLIST_URL = `${API_URL}/playlist`;
export const WATCH_URL = `${API_URL}/watch`;
