export const API_URL = 'http://localhost:5000';
export const ACCOUNT_URL = `${API_URL}/account`; // insert new account
export const PROFILE_URL = `${API_URL}/profile`; // get all profiles
export const CAST_URL = `${API_URL}/cast`; // insert new cast member
export const ITEM_URL = `${API_URL}/item`; // insert new content
export const FEEDBACK_URL = `${API_URL}/feedback`; // insert new feedback
export const PLAYLIST_URL = `${API_URL}/playlist`; // insert new playlist
export const WATCH_URL = `${API_URL}/watch`; // insert new watched content
export const CASH_URL = `${API_URL}/cash`; // get total amount gained
export const FAVORITES_URL = `${API_URL}/favorites`; // get favorites of a profile
export const RATEPROFILE_URL = `${API_URL}/rateprofile`; // get content with rating higher than X for someone
export const RATEOVERALL_URL = `${API_URL}/rateoverall`; // get content with rating higher that X overall

export const AVERAGERATING_URL = `${API_URL}/averagerating`; // get average rating
export const SAGA_URL = `${API_URL}/saga`; //get all content in a saga
export const TV_URL = `${API_URL}/tv`; // get all episodes in a tv series
